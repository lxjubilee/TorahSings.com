import { Overview } from '@/components/admin/Overview';

/** Metadata (including noindex) is set once for every /admin route in layout.tsx. */
export default function AdminOverviewPage() {
  return <Overview />;
}
